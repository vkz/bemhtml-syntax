block slider: {
  applyNext(a = 'first');
  if (bla) {
    test();
    applyNext({});
  } else {
    applyNext('first', {});
    return 'bla';
  }
  apply();
}

block button, mod round yes {
    default: applyNext(
      this.ctx.mods['only-icon'] = 'bla'
   )
}
